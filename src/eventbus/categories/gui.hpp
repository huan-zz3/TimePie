#pragma once
#include <string>

struct TimerModeSelected {
    uint32_t countminute;
    TimerModeSelected(uint32_t c) : countminute(c) {}
};

struct TimerCategorySelected {
    // uint32_t countminute;
    // TimerCategorySelected(uint32_t c) : countminute(c) {}
};